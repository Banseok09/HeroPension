
DROP TABLE LOGIN_LOG CASCADE CONSTRAINTS;

CREATE TABLE LOGIN_LOG(
	ID VARCHAR2(20) NOT NULL,
	VDATE DATE NOT NULL		
);

ALTER TABLE LOGIN_LOG
ADD CONSTRAINT FK_LOGIN_LOG_ID FOREIGN KEY(ID) 
REFERENCES MEMBER(ID);

