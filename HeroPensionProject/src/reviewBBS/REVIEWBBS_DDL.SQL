DROP TABLE REVIEWBBS  CASCADE CONSTRAINTS;

CREATE TABLE REVIEWBBS(
	REVIEWBBS_SEQ NUMBER(8),
	ID VARCHAR2(20) NOT NULL, 
	TITLE VARCHAR2(40) NOT NULL,
	CONTENT VARCHAR2(400) NOT NULL,
	RATE NUMBER(8) NOT NULL,
	REF NUMBER(8) NOT NULL,
	STEP NUMBER(8) NOT NULL,
	DEPTH NUMBER(8) NOT NULL,
	ROOM_SEQ NUMBER(8) NOT NULL,
	WDATE DATE NOT NULL,
	
	CONSTRAINT "PK_REVIEW_BBS_SEQ" PRIMARY KEY ("REVIEWBBS_SEQ"),
    CONSTRAINT "FK_REVIEW_ID" FOREIGN KEY("ID") REFERENCES MEMBER("ID")   -- 예약한 회원아이디
);

CREATE SEQUENCE REVIEWBBS_SEQ
INCREMENT BY 1
START WITH 1;

INSERT INTO MEMBER
VALUES ('park', 'park', '반석', '010-8924-2022', 'qkstjr09@gmail.com', 3, 0)